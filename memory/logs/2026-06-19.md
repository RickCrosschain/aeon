# 2026-06-19

## Telegram token message from user

User sent the bot token for @aeonrickbot via Telegram. Investigated:
- Verified token valid via getMe (bot: @aeonrickbot, id 8872999760).
- getUpdates empty, no webhook — inbound poller had already consumed the update.
- messages.yml poller (line ~517) only forwards when sender chat == TELEGRAM_CHAT_ID secret; since the message reached me, both TELEGRAM_BOT_TOKEN and TELEGRAM_CHAT_ID secrets are already set correctly and the notify loop works end-to-end.

Action: replied via ./notify confirming Telegram is fully configured, flagged that the token was posted in plaintext (chat history + Actions logs) and gave optional rotation steps (@BotFather /revoke + gh secret set). Did NOT store the token in any repo file.
